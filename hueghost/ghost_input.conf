# hue-ghost ghost player - minimal manual controls
# (default bindings are disabled; only these work)
#   ESC / q   close the ghost -> sync disables itself (hue-ghost on / tray to re-enable)
#   m         minimize to taskbar
#   f         toggle fullscreen <-> small corner window
ESC quit
q quit
Q quit
m window-minimize
f cycle fullscreen
